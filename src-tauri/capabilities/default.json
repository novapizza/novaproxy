{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main NovaProxy window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
