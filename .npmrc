# Every dependency here is public. Pinning the registry keeps a developer's
# CodeArtifact (or any other mirror) out of package-lock.json: those URLs need
# an auth token, which CI does not have, so a lockfile written through a mirror
# fails `npm ci` on the release runners.
registry=https://registry.npmjs.org/
